using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using game.client;

namespace GameTest
{
    [TestClass]
    public class CacheTest
    {

        /*
        [TestMethod]
        public void TestCache()
        {
            Cache c = new Cache("first");
            Assert.AreEqual("first", c.get());   
        }

        [TestMethod]
        public void testCacheEmpty()
        {
            Cache c = new Cache("first");
            c.get();
            Assert.ArtEqual(true, c.isEmpty);
        }

        [TestMethod]
        public void testBufferFull()
        {
            Cache c = new Cache("first");
            Assert.ArtEqual(false, c.isEmpty);
        }
        */
    }
}
